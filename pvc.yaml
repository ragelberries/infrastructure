apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-path-pvc-2
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ssd-local-path
  resources:
    requests:
      storage: 2Gi

